name = "immobild-contact"
main = "src/index.js"
compatibility_date = "2026-07-01"

[vars]
# Absender: recmo.de ist in Resend verifiziert (Status: Verified, EU-Region Irland),
# daher als Absenderdomain genutzt. Jede Adresse @recmo.de ist möglich (kein echtes
# Postfach nötig); Antworten gehen per Reply-To an den Kunden.
FROM_ADDRESS = "Immobild.ai <anfrage@recmo.de>"
# Empfänger der Anfragen.
TO_ADDRESS = "ismail.khudida@recmo.de"

# RESEND_API_KEY wird NICHT hier gespeichert, sondern als Secret gesetzt:
#   npx wrangler secret put RESEND_API_KEY
