name = "immobild-contact"
main = "src/index.js"
compatibility_date = "2026-07-01"

[vars]
# Absender: MUSS eine in deinem Resend-Account verifizierte Domain sein.
# Format: "Anzeigename <adresse@domain>". Passe die Domain an, falls in Resend
# eine andere Domain als immobild.ai verifiziert ist.
FROM_ADDRESS = "Immobild.ai <anfrage@immobild.ai>"
# Empfänger der Anfragen.
TO_ADDRESS = "ismail.khudida@recmo.de"

# RESEND_API_KEY wird NICHT hier gespeichert, sondern als Secret gesetzt:
#   npx wrangler secret put RESEND_API_KEY
