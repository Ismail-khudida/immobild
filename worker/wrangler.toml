name = "immobild-contact"
main = "src/index.js"
compatibility_date = "2026-07-01"

[vars]
# Absender: immobild.ai ist in Resend verifiziert (eigener Workspace "immobild",
# Status: Verified, EU-Region Irland). Antworten gehen per Reply-To an den Kunden.
FROM_ADDRESS = "Immobild.ai <anfrage@immobild.ai>"
# Empfänger der Anfragen.
TO_ADDRESS = "ismail.khudida@recmo.de"

# RESEND_API_KEY wird NICHT hier gespeichert, sondern als Secret gesetzt:
#   npx wrangler secret put RESEND_API_KEY
